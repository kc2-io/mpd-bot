[package]
name = "mpd-bot-view"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
slint = { version = "=1.17.1", default-features = false, features = ["std", "system-tray", "compat-1-2"] }

[build-dependencies]
slint-build = "=1.17.1"
